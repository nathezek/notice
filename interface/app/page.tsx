"use client";

export default function Home() {
    return (
        <div className="flex min-h-screen flex-col items-center p-12 font-sans dark:bg-neutral-900 dark:text-white">
            {/*
               The landing page is now empty except for the Navbar which
               centers itself when no search has happened.
            */}
        </div>
    );
}
